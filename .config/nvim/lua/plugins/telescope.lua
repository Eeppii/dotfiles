return {
  {
    "nvim-telescope/telescope.nvim",
    opts = {
      defaults = {
        -- Other good options: smart, truncate, shorten, shorten = 2 (does not work yet)
        path_display = { "truncate" },
      },
    },
  },
}
